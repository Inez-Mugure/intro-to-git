<!-- 
summary of git commands on this course

git init: used to initialize a repository 
touch <file name> used to create a new file
mkdir <project name> used to create a new project
git add <file name>
git status
git commit -m "<put message here starting with present tense verb then short description>"
git log
git add *.<file type>
git add -A (adds all files including hidden files)
git reset HEAD <file name> removes files from staging area
touch .gitignore creates .gitignore file
inside the file add file name or folder name of what you don't want to commit
git branch lists all branches
git checkout -b <branch name> creates and switches to new branch
git checkout <branch name> switches between branch
git merge <branch name> merges branches 
git branch -d <branch name> removes/deletes a branch

git remote add origin <url> used to initialize remote repo
git push -u origin master used to push local work to remote repo
-->